import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'stranerd_app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
